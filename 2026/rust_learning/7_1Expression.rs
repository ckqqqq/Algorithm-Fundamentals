fn main(){
    let x=5u8;
    let w=[1,2,3,4,5];
    let mut y=x+x;
    let y=x;
    let ww=w
    let mut z={
        y+x^2
    };// if you remove the semicolon, z will be a statement and () will be assigned to z.
    println!("y={:?},z={:?}",y,z);
    println!("x={}",x);
    // println!("ww={:?}",ww);
    
}